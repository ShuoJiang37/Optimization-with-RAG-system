#include <stdio.h>
#include <stdlib.h>

#define MaxNUM 100

int main( void )
{
    char tmp;
    char i,j,k = 0;
    unsigned char Num = 0;
    
    unsigned short Array[MaxNUM];
    unsigned short v = 0;

    // ??\??? 1?????????????????°?????????
    // ????????????????????\????????§??£?¶???????
    while( (tmp = getchar()) != '\n' )
    {
        Num = (Num * 10) + (tmp - '0'); // ??????????????°???????????????????¶?????????????
    };
    
    // ??\??? 2??????????????????????????????????????????
    for(k=0; k<Num; k++ )
    {
        // ??????????????????
        Array[k]=0;
    
        do{
            tmp = getchar();
            printf("%c",tmp);
            
            if(tmp == '\n') //???????????????????????????
            {
                break;
            }
            
            if( tmp != ' ' )
            {
                Array[k] = Array[k] * 10 + (tmp - '0');
            }
        }while(tmp != ' ' && tmp != '\n'); // ???????????£???????¬???????????????????
    }

    // ?????????
    for( i=1; i<Num; i++ )
    {
        v = Array[i];
        j = i - 1;
    
        // ??????????????????????????§????????¨??????1??????????????¨??\????????????
        while( j >= 0 && Array[j] > v )
        {
            Array[j+1] = Array[j];
            Array[j] = v;
            j--;
        }
    
        // ???????????????????????????????????????????????????????????????
        for(k=0; k<Num; k++ )
        {
            printf("%d",Array[k]);
            if( k == Num-1 )
            {
               printf("\n");    // ???????????????
            }
            else
            {
               printf(" ");
            }
        }
    }
    
    return 0;
}